package com.mustafakoceerr.justrelax.feature.onboarding.components

/**
 * UI'daki indirme seçeneklerini temsil eden enum.
 * Sadece UI seçimi (Radio Button mantığı) için kullanılır.
 */
enum class DownloadOptionType {
    STARTER, // Başlangıç Paketi
    FULL     // Tüm Kütüphane
}